package com.spaceship.model

enum class ShipClass { cruiser, frigate, destroyer, scout }
enum class CrewmateRole { captain, engineer, medic, pilot, gunner }
enum class MissionStatus { planned, active, completed, failed }
