default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV.fetch("DATABASE_URL") { "postgresql://spaceship:spaceship@localhost:5432/spaceship" } %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
